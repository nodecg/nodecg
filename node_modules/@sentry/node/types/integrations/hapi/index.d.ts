import type { Integration } from '@sentry/types';
import type { Server } from './types';
export declare const hapiErrorPlugin: {
    name: string;
    version: string;
    register: (serverArg: Record<any, any>) => Promise<void>;
};
export declare const hapiTracingPlugin: {
    name: string;
    version: string;
    register: (serverArg: Record<any, any>) => Promise<void>;
};
export type HapiOptions = {
    /** Hapi server instance */
    server?: Record<any, any>;
};
/**
 * Hapi Framework Integration
 */
export declare class Hapi implements Integration {
    /**
     * @inheritDoc
     */
    static id: string;
    /**
     * @inheritDoc
     */
    name: string;
    _hapiServer: Server | undefined;
    constructor(options?: HapiOptions);
    /** @inheritDoc */
    setupOnce(): void;
}
//# sourceMappingURL=index.d.ts.map